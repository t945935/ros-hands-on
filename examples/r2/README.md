# R2 實機程式（驗證後發布）

此目錄預留給依 ROSMASTER R2 控制板／原廠 ROS 套件版本實測的程式。加入任何會發送底盤命令的程式前，必須更新 `../../docs/r2-interface-inventory.md`，記錄 Topic、型別、單位、限制、停止行為與驗證步驟。

目前沒有可直接驅動 R2 的程式。請勿將 `../ros2_basics/` 的通用範例當成車體驅動。
